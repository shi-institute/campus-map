import DirectionsPane from './Directions/DirectionsPane.svelte';
import PlacePane from './Place/PlacePane.svelte';

const panes = { Directions: DirectionsPane, Place: PlacePane };

export default panes;
